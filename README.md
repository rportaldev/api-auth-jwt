# api-auth-jwt
Sistema backend desarrollado con Spring Boot que implementa autenticación mediante JWT, autorización basada en roles (ADMIN y USER), protección de endpoints y persistencia de datos en PostgreSQL.
